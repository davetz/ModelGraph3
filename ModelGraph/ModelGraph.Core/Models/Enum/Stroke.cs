
namespace ModelGraph.Core
{
    public enum Stroke : byte
    {
        IsSimple = 0, // shape's outline is drawn with solid lines
        IsDotted = 1, // shape's outline is drawn with dotted lines
        IsFilled = 2, // shape is drawn as a filled solid 
    }
}
